@~/.config/AGENTS.md

## Claude Code

The shared body above lives in `~/repos/agent-skills/instructions/AGENTS.md`; edit it there, not here. This file exists because Claude Code reads only `CLAUDE.md`, and it stays a real file rather than a symlink so Claude-only instructions can follow the import. Add them below this line.
